// ! git & github
// * git -> vcs, tool
// * github ->

// ! git command
// ? config
// * git config --global --list
// * git confif --global user.name""
// * git confif --global user.email"@.com"
// * git confif --global user.""
